interface Point{
    x: number, 
    y: number, 
    draw: () => void
}

let drawPoint = (point : {x: number, y: number}) => {
    //...
}

let getDistance = (pointA: number, pointB: number ) => {
    // ...
}

drawPoint({
    x: 1,
    y: 2

});


